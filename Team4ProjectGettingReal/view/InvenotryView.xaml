<UserControl x:Class="Team4ProjectGettingReal.View.InventoryView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Team4ProjectGettingReal.View"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <!-- StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Grid.Row="0"> -->
            <!-- <TextBlock Text="Inventory Header" VerticalAlignment="Center" FontSize="16"/> -->
        <!-- </StackPanel> -->

        <DataGrid ItemsSource="{Binding items}" Margin="0,0,0,30" Grid.Row="1"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="Register Sale" Width="75" Height="30" Command="{Binding GenerateSaleCommand}"/>
        </StackPanel>

    </Grid>
</UserControl>
